import {handleActions} from 'redux-actions';

export default handleActions({
    /**
     * your reducer goes here
     *
     * SOME_REDUCER: (state, action) => {
     *      return {
     *          ...state,
     *          yourChangedState: someValue
     *      }
     */
}, {
    /**
     * your initial state value goes here
     *
     * someDefaultState: someValue
     */
})